<?php 
    function calculateRectangleArea($length, $width) {
        return $length * $width;
    }

    echo calculateRectangleArea(10,5);

    function calculateSum($a, $b) {
        return $a + $b;
    }

    $sum = calculateSum(5, 10);
    echo $sum;
?>